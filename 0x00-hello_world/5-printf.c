#include <stdio.h>

/**
 * main - Entry point
 *
 *
 *'the program's prints a sentence with a new line at the end of it'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
